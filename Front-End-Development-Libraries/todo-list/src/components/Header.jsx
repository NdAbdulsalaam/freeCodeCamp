import React from "react";

const Header = () => {
    return(
        <>
            <h1>Todos</h1>
            <p>Items will be stored on the browser local disk</p>
        </>
    );
    };

export default Header