import React from 'react';

const TodoApp = () => {
    return (
        // We can use "<> </>" instead
        <React.Fragment>
            <h1>Hello world!</h1>
            <p>I am in a React Component!</p>
        </React.Fragment>
    );
};

export default TodoApp